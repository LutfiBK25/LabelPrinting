<Window x:Class="LabelDesigner.NewLabelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabelDesigner"
        mc:Ignorable="d"
        Title="New Label" 
        Height="250" 
        Width="300" 
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Width (inches)"/>
            <TextBox x:Name="WidthBox" Text="4"/>

            <TextBlock Text="Height (inches)" Margin="0,10,0,0"/>
            <TextBox x:Name="HeightBox" Text="6"/>

            <TextBlock Text="DPI" Margin="0,10,0,0"/>
            <TextBox x:Name="DpiBox" Text="203"/>
        </StackPanel>

        <Button Grid.Row="3"
                Content="Create Label"
                Height="30"
                Margin="0,15,0,0"
                Click="Create_Click"/>
    </Grid>
</Window>
